# Scenes

首次打开 Unity 后，请在这里新建一个空场景（`File → New Scene → Basic (Built-in)`），
命名为 `MainScene.unity`，在 Hierarchy 里创建一个空 GameObject 名为 `Bootstrap`，
挂上 `SceneBootstrap` 脚本即可按 Play 跑起来。

> `.unity` 场景文件由 Unity 创建（含大量 GUID 引用），不在仓库内提交初始版本。
